---
description: Diagnose this PRAXIS install — what is set up, what broke, how to fix it
---

Run `npx -y praxis-memory doctor` with the Bash tool and show me its output.

Then, in plain English:

1. Say whether PRAXIS is working correctly here — one sentence.
2. For anything that failed, explain what it means in practice (what the user
   loses while it is broken), and offer to run the fix for them.
3. If everything passed, say so plainly and stop. Do not invent concerns.

Note: this command only reads the local machine — nothing is sent anywhere.
