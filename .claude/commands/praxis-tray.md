---
description: Start (or stop) the axolotl in the system tray — Windows
---

Run `npx -y praxis-memory tray` with the Bash tool (pass `--stop` if I asked to quit it) and
show me the output. Then tell me in one sentence what the tray companion does:
the axolotl's glow mirrors this session's health, and its panel shows the
session state at a glance — no clicks needed.

On macOS or Linux, tell the user plainly: the tray is Windows-only for now —
it is on the roadmap.
