---
description: The AI context behind a commit — turn the trail on, or read it
---

Run `npx -y praxis-memory trace` with the Bash tool (pass through any arguments I gave, e.g.
`on`, `off`, `log`, or a commit hash) and show me the output. Then explain in
one short paragraph what it means: whether tracing is on, and — if a commit was
shown — what AI context that commit carries.
