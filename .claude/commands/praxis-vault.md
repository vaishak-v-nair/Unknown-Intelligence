---
description: Connect an Obsidian vault — sessions, commits and memory as linked notes
---

If I gave a path, run `npx -y praxis-memory vault <that path>` with the Bash
tool; with no path, run `npx -y praxis-memory vault` to show the current
setting. Show me the output, then
explain in one short paragraph what will now be written into the vault (a hub
note, a memory mirror, one note per session, one note per traced commit) and
that it is a one-way mirror — editing the vault copies changes nothing here.
