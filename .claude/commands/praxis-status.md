---
description: Show what PRAXIS remembers about this project
---

Read `.praxis/memory.md` and give me a short briefing:

- The project summary (from the `## Project` section).
- The last 3-5 entries from the `## Session Log`.

Do not modify the file. Just tell me what PRAXIS currently remembers.

If `.praxis/memory.md` does not exist in this project, stop and tell the user to run `npx praxis-memory` here first — one command, works on Windows, macOS and Linux.
