---
description: See exactly what leaves your machine (counts, never content)
---

Run `npx -y praxis-memory telemetry show` with the Bash tool (or `on` / `off` if I asked to
change it) and show me the output. Then confirm in one sentence what it means:
whether sharing is on or off, and that only anonymous usage COUNTS are ever
sent — never code, never words, never file names.
